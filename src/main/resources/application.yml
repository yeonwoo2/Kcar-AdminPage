spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kcar?serverTimezone=Asia/Seoul
    username: hyeonw
    password: hyeonw1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
#                show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace

